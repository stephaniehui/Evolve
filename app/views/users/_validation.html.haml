- if flash[:user_validation_errors]
  %div{:class => "alert alert-danger"}
    %p.validation The following error#{'s' unless flash[:user_validation_errors].size == 1} occurred:
    - flash[:user_validation_errors].each do |message|
      %li #{message}